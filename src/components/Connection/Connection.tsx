import { Component } from "solid-js"

const Connection: Component = () => (
  <div class="connection">
    <p class="connection__text">waiting for opponent to connect...</p>
  </div>
)

export default Connection
